<form action="/bookings" method="post">
  <button
    class="bg-gradient-to-r from-red-500 to-pink-500 text-white px-4 py-2 rounded-full text-sm font-semibold shadow-md hover:scale-105 hover:from-red-600 hover:to-pink-600 transition duration-300 ease-in-out">
    Book Now
  </button>
  <input type="text" name="houseid" value="<%= home._id %>" hidden>
</form>